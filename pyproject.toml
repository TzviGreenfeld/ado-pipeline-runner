[project]
name = "piperunner"
version = "0.1.7"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "azure-devops>=7.1.0b4",
    "dotenv>=0.9.9",
    "pyyaml>=6.0.2",
    "questionary>=2.1.1",
    "typer>=0.17.4",
]

[project.scripts]
piperun = "cli:app"

[tool.setuptools]
py-modules = ["cli", "pipeline_runner", "utils"]
